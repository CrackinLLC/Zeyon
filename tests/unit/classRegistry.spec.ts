import { describe, expect, it } from 'vitest';
import ClassRegistry from '../../src/classRegistry';

describe('Class Registry unit tests - ', () => {
  it('', () => {
    expect(ClassRegistry);

    // TODO: Create tests
  });
});
