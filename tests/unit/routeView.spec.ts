import { describe, expect, it } from 'vitest';
import RouteView from '../../src/routeView';

describe('Route View unit tests - ', () => {
  it('', () => {
    expect(RouteView);

    // TODO: Create tests
  });
});
