import { describe, expect, it } from 'vitest';
import CollectionView from '../../src/collectionView';

describe('Collection View unit tests - ', () => {
  it('', () => {
    expect(CollectionView);

    // TODO: Create tests
  });
});
