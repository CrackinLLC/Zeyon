import { describe, expect, it } from 'vitest';
import Emitter from '../../src/emitter';

describe('Emitter unit tests - ', () => {
  it('', () => {
    expect(Emitter);

    // TODO: Create tests
  });
});
