import { describe, expect, it } from 'vitest';
import Router from '../../src/router';

describe('Router unit tests - ', () => {
  it('', () => {
    expect(Router);

    // TODO: Create tests
  });
});
